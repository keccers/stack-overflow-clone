<%= form_for [@commentable, @comment], html: {class: "create-comment"} do |f| %>
<%= f.label :text, 'Comment Text' %>
<%= f.text_area :text, id: "editor" %>
<%= f.hidden_field :commenter_id, value: current_user.id %>
<%= f.submit  %>
<% end %>
